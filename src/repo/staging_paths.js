const staging = {
  temp: appRoot + "/staging/temp",
  beta: appRoot + "/staging/beta",
  master: appRoot + "/staging/master",
  static: appRoot + "/staging/static",
  zips: appRoot + "/staging/zips"
}

module.exports = staging
